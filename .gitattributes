# В репозитории всё хранится с LF — иначе Windows и Linux плодят пустые диффы
* text=auto eol=lf

# Двоичное — не текст: правку концов строк здесь git превратил бы в порчу файла
*.woff2 binary
*.png   binary
*.ico   binary
