# OS
.DS_Store
Thumbs.db
desktop.ini
# WSL помечает так файлы, скачанные из интернета
*:Zone.Identifier

# Editors
.vscode/
.idea/
*.swp
*~

# Исходники книг здесь не место — только конспекты. Складывать их в sources/:
# правило на папку не зависит ни от расширения, ни от его регистра, а список
# ниже — второй рубеж на случай, если файл лёг мимо папки.
sources/
*.pdf
*.epub
*.fb2
*.mobi
*.djvu

# Черновики: выжимки по главам и извлечённый из книг текст
draft/
*.draft.html
